apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: magicfit-backend-hpa
  namespace: magicfit
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: magicfit-backend
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 60
